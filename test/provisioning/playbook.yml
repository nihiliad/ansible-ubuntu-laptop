---
- hosts: all
  become: true
  vars:
    ansible_user: vagrant
  roles:
    - git
    - dropbox
    - powerline
    - vim

  tasks:
    #    - name: install dropbox
    #  apt: name=nautilus-dropbox state=present

