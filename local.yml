---
- hosts: 127.0.0.1
  connection: local
  roles:
    - standalone
    - chrome
    - fonts
    - zsh
    - direnv
    - zoom
    - anyenv
    - poetry
#    - { role: slack, slack_version: '4.23.0' }
