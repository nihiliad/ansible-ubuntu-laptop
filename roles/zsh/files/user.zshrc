# TODO: Fix this file test! No idea why it's not working.
#[[ ‐f /usr/local/etc/zsh.zshrc ]] && source /usr/local/etc/zsh.zshrc
source /usr/local/etc/zsh.zshrc

# Where did this come from?
# export MANPATH="/usr/local/man:$MANPATH"

#export PATH="$HOME/.anyenv/bin:$HOME/bin:$PATH"
#eval "$(anyenv init -)"

export VISUAL=vim
export EDITOR="$VISUAL"
