---
- hosts: all
  become: true
  vars:
    ansible_user: vagrant
  roles:
    - atom
    - backintime
    - bluetooth
    - git
    - powerline
    - vim
    - zsh
    - terminix
    - standalone-packages
