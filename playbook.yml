---
- hosts: all
  become: true
  vars:
    ansible_user: vagrant
  roles:
    - atom
    - backintime
    - bluetooth
    - git
    - dropbox
    - powerline
    - vim
    - zsh

  tasks:
    #    - name: install dropbox
    #  apt: name=nautilus-dropbox state=present

